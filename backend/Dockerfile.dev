# Backend Dockerfile per Development (con hot reload)
FROM node:20-alpine

WORKDIR /app

# Installa dipendenze
COPY package*.json ./
RUN npm ci

# Copia sorgenti
COPY . .

EXPOSE 3000

# Hot reload con tsx watch
CMD ["npm", "run", "dev"]
